import type { Auth, Session, User } from "lucia-auth";
import type { AstroGlobal } from "astro";
export declare class AuthRequest<A extends Auth> {
    private auth;
    private request;
    private cookies;
    constructor(auth: A, context: {
        request: Request;
        cookies: {};
    });
    validate: () => Promise<Session | null>;
    validateUser: () => Promise<{
        session: Session;
        user: User;
    } | {
        session: null;
        user: null;
    }>;
    setSession: (session: Session | null) => void;
}
export declare const handleLogoutRequests: (auth: Auth) => (context: {
    request: Request;
    cookies: AstroGlobal["cookies"];
}) => Promise<Response>;
