export const getTimeAfterSeconds = (seconds) => {
    return new Date().getTime() + 1000 * seconds;
};
export const isWithinExpiration = (millisecond) => {
    if (millisecond === null)
        return false;
    const currentTime = Date.now();
    if (currentTime > millisecond)
        return false;
    return true;
};
